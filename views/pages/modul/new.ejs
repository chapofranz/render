<div class="container">
    <h1>Modul hinzufügen</h1>
    <form action="/module/create" method="POST" id="newModuleForm">
        <div class="form-group">
            <label for="name">Name</label>
            <input type="text" class="form-control" id="name" name="name" required>
        </div>
        <div class="form-group">
            <label for="beschreibung">Beschreibung</label>
            <input type="text" class="form-control" id="beschreibung" name="beschreibung" required>
        </div>
        <div class="form-group">
            <label for="studiengang" id="label">Studiengang</label>
            <select name="studiengang[]" id="studiengang" class="upload-form" required multiple>
                <option value="" disabled>Wählen Sie einen oder mehrere Studiengänge aus</option>
                <% studiengaenge.forEach(function(studiengang) { %>
                    <option value="<%= studiengang.id %>">
                        <%= studiengang.name %> (<%= studiengang.id %>)
                    </option>
                <% }); %>
            </select>
        </div>
        
        <button type="submit" class="btn btn-primary">Modul hinzufügen</button>
    </form>
</div>
