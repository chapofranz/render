<div class="container">
    <form action="/studiengang/<%= studiengang.id %>/update" method="post">
        <div>
            <div class="form-group">
                <label class="col-form-label-lg">Name</label>
                <input type="text" class="form-control" maxlength="80" value="<%= studiengang.name %>" name="name">
            </div>
            <div class="form-group">
                <label class="col-form-label-lg">Kürzel</label>
                <input type="text" class="form-control" maxlength="80" value="<%= studiengang.kuerzel %>" name="kuerzel">
            </div>
        </div>
        <button type="submit" class="btn btn-primary">Aktualisieren</button>
    </form>
</div>
